<Window x:Class="LaptopQaUsbBuilder.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Partition Settings" Width="790" Height="560" MinWidth="720" MinHeight="500"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" FontFamily="Segoe UI" Foreground="#17272D">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#687F87"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="Padding" Value="18,9"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="18" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter>
        </Style>
    </Window.Resources>
    <Border Background="#F4F5F1" BorderBrush="#9BB0B7" BorderThickness="1" CornerRadius="26">
        <Grid Margin="28,22">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="44"/></Grid.ColumnDefinitions>
                <StackPanel><TextBlock Text="Partition Settings" FontSize="28" FontWeight="Light"/><TextBlock Text="Configure the GPT layout used for future builds." Foreground="#526970"/></StackPanel>
                <Button Grid.Column="1" Content="x" Click="Cancel_Click" Foreground="#17272D" Background="Transparent" FontSize="18" Padding="4"/>
            </Grid>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,22,0,12">
                <TextBlock Text="Partition count" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <ComboBox x:Name="CountPicker" Width="80" Height="36" FontSize="14" SelectionChanged="CountPicker_SelectionChanged"/>
                <TextBlock Text="The final partition always uses the remaining disk space." Foreground="#526970" VerticalAlignment="Center" Margin="18,0,0,0"/>
            </StackPanel>
            <DataGrid x:Name="PartitionGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                      CanUserReorderColumns="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeaderWidth="0"
                      Background="#FCFDFB" BorderBrush="#9BB0B7" FontSize="14" RowHeight="42">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Number}" IsReadOnly="True" Width="45"/>
                    <DataGridTextColumn Header="Volume label" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                    <DataGridTextColumn Header="Size (MB, GB, or Remaining)" Binding="{Binding SizeText, UpdateSourceTrigger=PropertyChanged}" Width="225"/>
                    <DataGridComboBoxColumn x:Name="FormatColumn" Header="Format" SelectedItemBinding="{Binding FileSystem, UpdateSourceTrigger=PropertyChanged}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Grid.Row="3" Text="Size examples: 50 MB, 20 GB. Use Remaining only for the final partition. FAT32 labels allow 11 characters; exFAT allows 15; NTFS allows 32." Foreground="#526970" TextWrapping="Wrap" Margin="2,12,0,0"/>
            <Grid Grid.Row="4" Margin="0,18,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="110"/><ColumnDefinition Width="110"/></Grid.ColumnDefinitions>
                <Button Content="Restore Defaults" Click="Defaults_Click" HorizontalAlignment="Left"/>
                <Button Grid.Column="1" Content="Cancel" Click="Cancel_Click" Margin="8,0"/>
                <Button Grid.Column="2" Content="Save" Click="Save_Click" Background="#147A4B" Margin="8,0,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
